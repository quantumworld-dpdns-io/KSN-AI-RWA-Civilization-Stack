# Scripts

## Generate simulation report

```bash
pnpm exec tsx scripts/generate-report.ts
```

The report prints a JSON summary of all sample assets, their KSN score, agency state, autonomy risk, and yield distribution.
