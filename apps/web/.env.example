# Sui microgrid: works out of the box against the committed testnet deployment
# (apps/web/src/generated/sui.testnet.json) via the server route /api/chain/sui.
# NOTE: public Sui fullnodes deprecated JSON-RPC — reads use the GraphQL endpoint.
# Override only to point at a different object or a private/paid GraphQL endpoint:
# SUI_GRAPHQL_URL=https://graphql.testnet.sui.io/graphql
# SUI_MICROGRID_ID=0xe7b809c14e1cb43efa3a53634c6b9451889fe6155a4c5b10bd1a5f97c342af6e

# Server-side URL for the Oracle simulator API.
# Local development:
ORACLE_API_URL=http://127.0.0.1:8787

# Docker Compose alternative:
# ORACLE_API_URL=http://oracle-sim:8787
# Production example:
# ORACLE_API_URL=https://<your-choreo-oracle-base-url>

# ORACLE_URL is supported as a legacy fallback. Prefer ORACLE_API_URL.

# Sepolia RPC URL for live on-chain reads in the Sepolia contracts view (viem).
# Falls back to a public endpoint if unset. Use your own Alchemy/Infura URL for reliability.
SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/replace-me
